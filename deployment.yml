name: Deploy to Azure

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy to Azure
      run: |
        # Fügen Sie hier die Azure-Bereitstellungsanweisungen ein, z.B. mit Azure CLI-Befehlen
        # Beispiel: az webapp up -n your-webapp-name -g your-resource-group -l your-location